package com.example.urticare20.ui.theme

import androidx.compose.ui.graphics.Color

// Premium White/Light Mode Colors (Variable names kept for codebase compatibility)
val AmoledBlack = Color(0xFFFFFFFF)  // Pure White Background
val DarkSurface = Color(0xFFFAFAFA)  // Light Grey Surface
val DarkCard = Color(0xFFFFFFFF)     // Card Background (White)
val DarkBorder = Color(0xFFE2E2E2)   // Light border color

// Custom Tracker Color Tokens (Themed directly from the Urticare Logo!)
val CoralPink = Color(0xFF814B92)      // Flare Up (Purple #814b92)
val PastelIceBlue = Color(0xFF1B8097)  // Antihistamine Taken (Teal #1b8097)
val SoftYellow = Color(0xFF509729)     // Cortisone/Steroid (Green #509729)
val SoftPurple = Color(0xFF814B92)     // Alternative Injections & Urti Chat (Purple #814b92)

// Typography and Accent Colors
val LightGray = Color(0xFF111111)    // Primary Text (Was light gray, now dark gray/black)
val MutedGray = Color(0xFF666666)    // Secondary/Muted Text (Was muted gray, now medium gray)
val DarkGray = Color(0xFFEEEEEE)     // Accents and low contrast backgrounds (Was dark gray, now light gray)
val AlertRed = Color(0xFFD9534F)     // Contrasting red warning
val LinkBlue = Color(0xFF007AFF)     // Standard blue link